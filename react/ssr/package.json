{
  "type": "module",
  "name": "css-modules-test",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "esbuild": "0.21.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "scripts": {
    "bundle": "node ./build.js",
    "transpile": "esbuild ./src/* --outdir=./dist",
    "dev": "yarn bundle && yarn transpile && node ./server.js"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  }
}
